В датасете nebius собрали 80 036 прогонов SWE-agent на задачах из GitHub. Он хранится в parquet со следующей структурой:

instance_id: идентификатор задачи в формате "owner__repo-number"
trajectory: список шагов агента, внутри обычные словари
exit_status: статус завершения прогона, например "submitted", "exit_context" или "early_exit"

На один instance_id часто приходится несколько строк. Это разные попытки решить задачу. Индекс попытки — traj_idx (глобальный, 0–80035).

Внутри траектории каждый шаг состоит из двух полей:

role: указывает на автора реплики. "ai" когда думает или действует агент, "user" когда отвечает терминал или среда.
text: само содержимое шага.

Они всегда чередуются: ai -> user -> ai -> user.

Спарсенные ошибки сложили в `errors_invalid_invocation.json`. Формат — плоский список (одна запись = один факт ошибки):

```json
{
  "A": [
    {
      "instance_id": "django/django-12345",
      "traj_idx": 12345,
      "step_idx": 9,
      "category": "A",
      "text": "FileNotFoundError: [Errno 2] No such file or directory: 'X'",
      "normalized_pattern": "FileNotFoundError: No such file or directory: 'X'",
      "occurrence_in_traj": 2,
      "is_first_occurrence_in_traj": false
    },
    ...
  ],
  "B": [...],
  "E1": [...],
  "E2": [...]
}
```

Поля для анализа Time-to-First-Failure и Thrashing:
- `occurrence_in_traj`: порядковый номер ошибки в траектории (по нормализованному паттерну)
- `is_first_occurrence_in_traj`: True если первое вхождение

**Отличие от старого формата:** Старый формат (до 2026-05-29) группировал ошибки по (instance_id, pattern_hash) — одна запись = много вхождений. Новый плоский формат позволяет анализировать повторные попытки внутри одного instance_id.