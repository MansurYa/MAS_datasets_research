**ТЗ №5 — Финальный отчёт: сборка, исправления, графики, документ**

---

## Контекст проекта

Финальное задание в серии по анализу ошибок мультиагентных систем. Все предыдущие ТЗ (1-4.8) выполнены. Нужно:
- Исправить известные ошибки в данных
- Собрать итоговый CSV
- Сгенерировать графики
- Написать отчёт на русском языке

**Существующие данные и скрипты:**
- `data/trail_errors.csv` — ошибки TRAIL (816 аннотаций из 143 траекторий)
- `data/who_and_when_handcrafted_classified.csv` — Who&When Hand-Crafted (58 записей)
- `data/errors_classified.csv` — AgentRx (334 failures)
- `data/keyword_positions.csv` — позиции ошибок из keyword search
- `data/keyword_search_results.csv` — P(trajectory) из keyword search
- `data/all_errors_final.csv` — текущая сводная таблица (37 строк, с ошибками)
- `data/distributions_v2.csv` — подгонка распределений для TRAIL/AgentRx/Who&When
- `data/distributions_extended.csv` — подгонка для keyword search (включая Pareto/Gamma/Lomax)
- `data/stats_full_v2.csv` — P(traj), P(msg) для TRAIL/AgentRx/Who&When
- Скрипты: `tz4_8_trail_extract.py`, `tz4_8_stats.py`, `tz4_distributions.py`, `tz4_7_heavy_tails.py`
- Графики: `data/plots/hist48_*.png`, `data/plots/hist_kw_*.png`, `data/plots/qq_*.png`

---

## Часть A. Исправление данных

### A1. Потерянные TRAIL траектории

В оригинале TRAIL содержит 148 траекторий и 841 аннотацию. У нас загрузилось 143 траектории и 816 аннотаций. Потеряно 5 траекторий и 25 аннотаций.

Открыть `tz4_8_trail_extract.py`, найти место где теряются траектории. Вероятные причины:
- Файлы не прочитаны (ошибка пути, невалидный JSON)
- Аннотации отфильтрованы (категория "unknown" или пустая)

Исправить и перезапустить. Цель: 148 траекторий, ~841 аннотация (допустимо исключение unknown).

### A2. Добавить ошибки с n=0

Добавить в итоговую таблицу 7 ошибок из `fault_mode_analysis_and_classification_ru.html` которые не имеют эмпирических данных:

| error_id | name_ru | description_ru | modeling_class |
|---|---|---|---|
| hardware_degradation | Деградация оборудования | Постепенное ухудшение характеристик оборудования со временем | 4 |
| gpu_throttling | Троттлинг GPU | Снижение частот GPU из-за перегрева | 4 |
| correlated_ssd_failure | Коррелированные сбои SSD | Одновременные отказы нескольких накопителей в стойке | 4 |
| network_power_failure | Сетевые и электросбои | Отказ сетевой инфраструктуры или электропитания | 4 |
| bad_retry_policy | Неверная политика повторов | Некорректные параметры backoff/jitter вызывают retry storm | 2 |
| kv_transfer_failure | Сбой передачи KV-кэша | Ошибка при передаче кэша между узлами кластера | 2 |
| memory_bandwidth_bottleneck | Узкое место пропускной способности памяти | Конкуренция запросов за HBM снижает производительность | 2 |

Для всех: source="теоретическая", все числовые поля = пустые, data_quality="нет данных", insufficient_data=True, fit_conclusion="нет данных", plots="нет".

### A3. Подписать параметры распределений

В столбце `best_dist_params` заменить голые числа на подписанные:
- Gamma: `"shape=2.05, loc=0, scale=14.49"`
- Exponential: `"loc=7.0, scale=24.43"`
- Weibull: `"shape=1.56, loc=0, scale=50.29"`
- LogNormal: `"s=0.89, loc=0, scale=28.10"`
- Beta: `"a=1.34, b=109.11, loc=0.02, scale=17.03"`
- Pareto: `"b=0.55, loc=0, scale=2.0"`
- Lomax: `"c=11.19, loc=0, scale=194.72"`

### A4. Убрать длинные тире

Во всех CSV файлах: заменить символы `—`, `–` на пустую строку. Пустые значения в CSV = просто пустая ячейка (два запятых подряд).

### A5. Добавить столбец ссылок на графики

В итоговый CSV добавить последний столбец `plots` — имена файлов графиков через точку с запятой:
- Если есть гистограмма: `hist_{error_id}_{source}.png`
- Если есть Q-Q plot: `qq_{error_id}_{source}.png`
- Если нет графиков: пустая ячейка

---

## Часть B. Графики

Все графики сохранять в `report/plots/`. Подписи на русском. Шрифт читаемый (размер ≥ 10). Формат PNG, dpi=150.

### B1. Индивидуальные графики

Для каждой ошибки с step_n ≥ 20 из TRAIL, AgentRx, Who&When HC:

**Гистограмма позиций (абсолютная):**
- X: номер шага
- Y: число вхождений
- Заголовок: `"{name_ru} ({source}, n={step_n})"`
- Если есть успешная подгонка (fit_conclusion содержит "best_fit") — наложить кривую распределения поверх гистограммы (density=True, цвет красный)
- Сохранить как `hist_{error_id}_{source}.png`

Для keyword search надёжных пар (tool_web_failure/nebius, resource_not_found/nebius, tool_timeout/itbench, permission_error/terminalbench, memory_error/terminalbench) — аналогично.

**Q-Q plot** — только для ошибок где best_dist_ks_p > 0.05:
- Сохранить как `qq_{error_id}_{source}.png`

### B2. Сводные графики

**График 1: `summary_p_trajectory.png`**
Горизонтальный барплот. Одна строка на каждую уникальную ошибку (error_id). Если ошибка встречается в нескольких источниках — показать несколько баров рядом (группировка по source). Цвет баров по modeling_class:
- Класс 1: серый
- Класс 2: синий
- Класс 3: зелёный
- Класс 4: красный

X: P(trajectory). Y: name_ru. Подписи значений на барах. Сортировка по P(trajectory) убывающая.

**График 2: `summary_by_class.png`**
Круговая диаграмма (pie chart): сколько уникальных error_id в каждом классе 1-4. Подписи: "Класс 1: невозможно (N)", "Класс 2: напрямую (N)", "Класс 3: статистически (N)", "Класс 4: нецелесообразно (N)".

**График 3: `summary_n_observations.png`**
Горизонтальный барплот. X: log10(n_trajectories_with_error + 1). Y: name_ru + source. Цвет по data_quality. Вертикальная линия на x = log10(20) с подписью "порог достаточности (n=20)". Вертикальная линия на x = log10(3000) с подписью "порог надёжной подгонки (n=3000)".

**График 4: `summary_step_position.png`**
Box plot нормализованных позиций (step / trajectory_length) для всех ошибок с step_n ≥ 20. Одна коробка на error_id. Сортировка по медиане. Подписи на русском.

---

## Часть C. Итоговый CSV

Файл: `report/all_errors_final.csv`

Столбцы (в этом порядке):

```
error_id, name_ru, description_ru, source, modeling_class, modeling_class_reason_ru,
n_trajectories_with_error, n_trajectories_total,
p_trajectory, p_traj_ci_lower, p_traj_ci_upper,
total_steps, p_message, p_msg_ci_lower, p_msg_ci_upper,
step_mean, step_median, step_std, step_n,
best_distribution, best_dist_params, best_dist_ks_p, fit_conclusion_ru,
data_quality_ru, insufficient_data, plots
```

`modeling_class_reason_ru` — на русском.

`fit_conclusion_ru`:
- `"подгонка найдена"` — хотя бы одно распределение не отвергнуто (KS p > 0.05)
- `"параметрическое не подходит"` — все отвергнуты при n > 100, рекомендуется эмпирическое распределение
- `"низкая мощность теста"` — n < 100, тест не различает распределения
- `"нет данных"` — n = 0

`data_quality_ru`:
- `"экспертная разметка"` — TRAIL (148 траекторий, ручная аннотация экспертами)
- `"аннотация на уровне шагов"` — AgentRx (334 failures, разметка категорий и шагов)
- `"keyword matching"` — Who&When HC (58 записей, классификация по ключевым словам свободного текста, покрытие ~71%)
- `"keyword search в траекториях"` — nebius/ITBench/terminalbench (поиск ключевых слов в ответах среды)
- `"нет данных"` — теоретические ошибки без эмпирических наблюдений

Пустые значения — пустая ячейка, без тире.

---

## Часть D. Отчёт на русском

Файл: `report/fault_analysis_report.md`

### Структура документа:

---

**Раздел 1. Введение**

Цель исследования, перечень датасетов с кратким описанием каждого (3-4 предложения), число траекторий.

Таблица датасетов:

| Датасет | Траекторий | Тип разметки | Что извлечено |
|---|---|---|---|

---

**Раздел 2. Методология**

Подраздел 2.1. Определения:

- **P(траектория)** — доля траекторий, содержащих хотя бы одно вхождение ошибки данного типа: P = k / n, где k - число траекторий с ошибкой, n - общее число траекторий в источнике.

- **P(сообщение)** — доля шагов траекторий, содержащих ошибку: P = m / S, где m - суммарное число шагов с ошибкой, S - суммарное число шагов во всех траекториях источника. Ограничение: шаги внутри одной траектории не являются независимыми наблюдениями, поэтому доверительный интервал формально занижен.

- **Wilson 95% доверительный интервал** — интервальная оценка биномиальной пропорции, устойчивая при малых n и крайних p (в отличие от интервала Вальда). Формула:
  ```
  center = (p + z^2/(2n)) / (1 + z^2/n)
  margin = z * sqrt(p(1-p)/n + z^2/(4n^2)) / (1 + z^2/n)
  CI = [center - margin, center + margin], z = 1.96
  ```

- **Критерий Колмогорова-Смирнова (KS-тест)** — непараметрический тест согласия. Статистика D = max|F_emp(x) - F_theor(x)|. Ограничение: при оценке параметров по тем же данным (сложная гипотеза) p-value завышен. При n >> 3000 мощность теста высока и отвержение H0 информативно. При n < 100 мощность низка и неотвержение H0 не означает хорошую подгонку.

- **Порог достаточности данных (n >= 20)** — минимальное число траекторий с ошибкой для осмысленной статистики. При n < 20 Wilson CI слишком широк (более 30 п.п.), подгонка распределений невозможна.

- **Порог надёжной подгонки (n >= 3000)** — минимальный объём для уверенных выводов о виде распределения. При n < 3000 KS-тест не различает распределения; результаты подгонки носят ориентировочный характер.

Подраздел 2.2. Классы моделируемости:

| Класс | Название | Определение | Пример |
|---|---|---|---|
| 1 | Невозможно моделировать | Воспроизведение ошибки требует полного прогона LLM. Нельзя инжектировать как структурное событие в IR-граф | Галлюцинация, ошибка в коде |
| 2 | Моделируется напрямую | Ошибка воспроизводится изменением параметров или структуры IR-графа без статистических допущений | Потеря KV-кэша, таймаут |
| 3 | Моделируется статистически | Ошибка описывается вероятностью и распределением момента появления. Инжектируется как случайное событие | Resource Abuse, tool_web_failure |
| 4 | Нецелесообразно моделировать | Технически возможно, но выходит за рамки проекта | Деградация оборудования |

Подраздел 2.3. Качество источников данных:

| Категория | Определение | Источники |
|---|---|---|
| Экспертная разметка | Ошибки размечены вручную экспертами на уровне отдельных участков траектории | TRAIL (148 трейсов) |
| Аннотация на уровне шагов | Типы ошибок и номера шагов размечены аннотаторами | AgentRx (73 траектории) |
| Keyword matching | Классификация свободного текста (mistake_reason) по ключевым словам | Who&When HC (58 записей) |
| Keyword search в траекториях | Поиск паттернов ошибок (HTTP-коды, traceback, timeout) в ответах среды выполнения | nebius, ITBench, TerminalBench |
| Нет данных | Теоретическая ошибка без эмпирических наблюдений | 7 ошибок классов 2, 4 |

---

**Раздел 3. Сводная таблица ошибок**

Таблица из CSV, сгруппированная по modeling_class. Компактная версия (без CI, без step_std):

| error_id | Название | Класс | Источник | n | P(traj) | CI 95% | Распределение | Графики |

После таблицы — ссылка на полный CSV.

---

**Раздел 4. Анализ по классам**

Для каждого класса 1-4:
- Список ошибок с кратким описанием
- Для классов 2-3: подробный разбор каждой ошибки с данными, распределениями, графиками
- Для класса 1: общая характеристика (все семантические, зависят от LLM)
- Для класса 4: причина нецелесообразности

---

**Раздел 5. Результаты подгонки распределений**

Для каждой ошибки классов 2-3 с n >= 20:
- Описательная статистика (mean, median, std, p25, p75)
- Таблица всех проверенных распределений с KS p-value
- Вывод: какое распределение лучшее (или "рекомендуется эмпирическое CDF")
- Встроенный график (гистограмма + кривая если подгонка есть)

---

**Раздел 6. Ограничения**

Перечень всех ограничений (одним списком):
- TRAIL: 148 траекторий, малая выборка для подгонки
- Who&When HC: keyword matching покрытие ~71%
- Keyword search: ложные срабатывания (числа как номера строк, имена классов как ошибки памяти)
- KS-тест: сложная гипотеза, p-value завышен
- P(сообщение): нарушение независимости шагов
- Для ошибок классов 2, 4 с n=0: параметры требуют экспертной оценки или литературных данных

---

**Раздел 7. Приложения**

- Маппинг TRAIL категорий на наши error_id
- Правила keyword matching для Who&When
- Правила keyword search для nebius/ITBench/TerminalBench
- Матрица надёжности keyword search (из tz4_5_category_interpretation.md)

---

## Результаты

| Файл | Описание |
|---|---|
| `report/fault_analysis_report.md` | Главный документ на русском |
| `report/all_errors_final.csv` | Итоговая таблица (исправленная) |
| `report/plots/summary_p_trajectory.png` | Сводный: P(traj) по ошибкам |
| `report/plots/summary_by_class.png` | Сводный: распределение по классам |
| `report/plots/summary_n_observations.png` | Сводный: объём данных |
| `report/plots/summary_step_position.png` | Сводный: box plot позиций |
| `report/plots/hist_*.png` | Индивидуальные гистограммы |
| `report/plots/qq_*.png` | Q-Q plots для успешных подгонок |